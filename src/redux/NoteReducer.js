const initState = {
  memo: [
    {
      title: '오늘의 일기',
      content: '어제 술마셔서 힘들다 졸리다...',
    },
  ],
};

const NoteReducer = (state = initState) => {
  return state;
};

export default NoteReducer;
